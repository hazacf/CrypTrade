# Copy to .env and fill in. NEVER commit your real .env (it is gitignored).
# Use TESTNET keys first (exchange.testnet: true in config.yaml).
EXCHANGE_API_KEY=your_key_here
EXCHANGE_API_SECRET=your_secret_here
